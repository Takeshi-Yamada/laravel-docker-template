# Laravel Lesson レビュー①

## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か
SELECT * FROM todos;

### Todoモデルのallメソッドの返り値は何か
todosテーブル内のレコード全て。
配列ではなくコレクションクラスで返ってくる。

### 配列の代わりにCollectionクラスを使用するメリットは
コレクションクラスのメソッドが使える。(first,count等)

### view関数の第1・第2引数の指定と何をしているか
第1引数：Viewファイルのパス
第2引数：グローバル変数

### index.blade.phpの$todos・$todoに代入されているものは何か
$todos：DBから取得したtodosレコードをグローバル変数に格納したもの
$todo：レコードを最初から１件ずつ取り出したもの

## Todo作成機能

### Requestクラスのallメソッドは何をしているか
Viewファイル（フォーム）から受け取った入力値全てを取り出している

### fillメソッドは何をしているか
todoインスタンス（連想配列）のキーに対応するものがあれば全て代入している

### $fillableは何のために設定しているか
脆弱性を排除するため
具体的には認めていないカラムの入力を弾いており、なりすまし等を防ぐ効果がある

### saveメソッドで実行しているSQLは何か
INSERT INTO todos('content') VALUES ('入力値');

### redirect()->route()は何をしているか
一覧画面へのリダイレクトを行っている。
リダイレクトしない場合、レコード登録後に白い画面が出てきてしまい操作が出来なくなってしまうため

## その他

### テーブル構成をマイグレーションファイルで管理するメリット
チームで同じテーブル環境で開発が行えるから

### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか
up():`php artisan migrate`
down():`php artisan migrate:rollback`

### Seederクラスの役割は何か
テスト用のデータを作成する
手作業でデータ登録が不要、チームで初期データをそろえることができるのがメリット

### route関数の引数・返り値・使用するメリット
（例：Route::get('/todo', 'TodoController@index')->name('todo.index');）
引数：指定したルート名 （例：`todo.index`）
返り値：指定したパス （例：`'/todo'`）

メリット：ルーティングを変更するときに修正が1行で済む、長いパスを書く必要がない、直感的なパス名をつけることができる

### @extends・@section・@yieldの関係性とbladeを分割するメリット
どのページでも表示されるような要素（ヘッダーやフッター等）のコードを共通化できる
`@yield`で場所の指定、`@extends`で継承ファイルの指定、`@section`で挿入するコードの指定

・親ファイル内で子ファイルを挿入したいところに`@yield('名前')`を記載
・子ファイルの最初に`@extends(親ファイル名)`と記載し継承するファイルを指定する
・`@yield('名前')`に挿入したいコードを`@section('名前') ~ @endsection`で囲む
※`@yield`と`@section`の引数に記載する名前は統一する必要がある

メリットは修正の工数や、（被っているコードを共通化できるため）ファイル毎の文章量を省略できる


### @csrfは何のための記述か
CSRF対策用のセキュリティコード
自動でトークンを発行し、Laravel側が検証もしてくれる

### {{ }}とは何の省略系か
{!! !!}の省略形（但しXXS対策はしてくれていない）
ページを描写する際に`<>`等のhtmlで特殊な振る舞いをする文字を別の文字に変換してくれる